name: Update MiGu EPL playlist

# Disabled: the automatic updater was failing and sending repeated notifications.
# Re-enable only after the updater is fixed and tested.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Automatic MiGu EPL updates are currently disabled."
